<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springboot.mapper.TransactionMapper">

    <resultMap id="BaseResultMap" type="com.example.springboot.entity.Transaction">
            <id property="id" column="id" jdbcType="VARCHAR"/>
            <result property="userId" column="user_id" jdbcType="INTEGER"/>
            <result property="productId" column="product_id" jdbcType="INTEGER"/>
            <result property="createTime" column="create_time" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,seller_id,buyer_id,
        product_id,create_time
    </sql>
    <delete id="deleteByUserId">
        delete from transaction where user_id=#{userId}
    </delete>

    <select id="selectPageById" resultType="map">
        select t.*,u.username buyer_username,q.username seller_username from transaction t inner join user u inner join (select p.id,u.username from product p inner join user u on p.user_id = u.id) q on t.user_id=u.id and t.product_id=q.id
        <if test="id != null">
            where t.id=#{id}
        </if>
        order by t.id desc
    </select>





</mapper>
